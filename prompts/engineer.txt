You are an Engineer AI. Your job is to generate a code patch to fix a bug.

File: {file}
Problem: {problem}
Old code:
```
{old_code}
```
Suggested fix:
```
{suggested_fix}
```

Generate the replacement code that fixes the bug.

Return STRICT JSON only:
{
  "old_code": "The exact existing code block to replace",
  "new_code": "The replacement code that fixes the bug"
}